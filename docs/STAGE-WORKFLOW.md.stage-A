# STAGE-WORKFLOW — stage-A touch-up (post-`52f7298`)

> **파일:** `docs/STAGE-WORKFLOW.md.stage-A`  
> 원본은 이미 **정식화**됨. 아래는 헤더/이력 **정리 제안만** (MC 병합).

---

## 패치

1. 파일 상단 상태 블록을 다음으로 교체:

```markdown
> **파일:** `docs/STAGE-WORKFLOW.md` (정식)  
> **승격:** INTEGRATE `52f7298`  
> **작성 계보:** 트랙 A stage → MC  
> **추가 변경:** 트랙은 `*.stage-<TRACK>` 만; 원본은 MC만
```

2. “MC 통합 대기” / “초안” 문구 삭제.

3. §6 보강: M/N 해금 코드는 `52f7298` 이후 **원본 live**; 추가 변경은 여전히 `.stage-M` / `.stage-N`.

4. 변경 이력:

```markdown
| 2026-09-04 | MC `52f7298` 승격 · 정식화 |
| 2026-09-04 | stage-A: 헤더 정리 · 다음 Tracks 큐는 H→J→D→L5-R/B→I↔K |
```

---

## 변경 이력 (stage-A)

| 날짜 | 내용 |
|------|------|
| 2026-09-04 | 정식화 후 헤더 touch-up 제안 |
