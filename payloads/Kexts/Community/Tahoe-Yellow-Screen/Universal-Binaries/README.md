# Universal-Binaries overlay slots (Track F)

Drop licensed PSP trees under `12.5-25/` / `12.5-26/` / `RenderBox-25/`.
See parent SOURCE.md and `RenderBox-25/SOURCE.md`. README-only dirs are not injectable.
Metallib/kext binaries stay gitignored — use `Tools/fetch_renderbox25.py`.
