# Tahoe Metallib Bytecode & Opaque Shaders

Investigation of compiled Metal library (`.metallib`) structures and opaque shader bytecode generated by the macOS 26 SDK.

## Analysis

- Format versioning in macOS 26 metallibs requires updated symbol metadata parsing.
- Shaders targeting pre-AVX CPUs require compiler flags preventing emission of advanced vector extensions in CPU-side shader compilation workers.
