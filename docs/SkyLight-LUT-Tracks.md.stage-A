# SkyLight-LUT-Tracks — stage-A (post-INTEGRATE `52f7298`)

> **파일:** `docs/SkyLight-LUT-Tracks.md.stage-A`  
> **원본 직접 수정 금지.** MC가 원본 `SkyLight-LUT-Tracks.md`에 병합.  
> **기준 커밋:** [52f7298](https://github.com/NiSeullent/26x86/commit/52f7298) `feat(extreme-INTEGRATE): merge stage-A docs and live M/N/F unlock paths`

---

## MC에 전달 — 원본에 반영할 패치

### 1) 헤더에 INTEGRATE 스탬프 추가

```markdown
> **마지막 INTEGRATE:** `52f7298` (A docs · M 3802 · N Non-Metal · F PSP live)
> **STAGE-WORKFLOW:** 정식화됨 — [STAGE-WORKFLOW.md](./STAGE-WORKFLOW.md)
```

### 2) 「INTEGRATE 우선순위」 절을 아래로 **교체**

```markdown
### INTEGRATE 완료 (고정 순서 1–4)

| 순 | 트랙 | 커밋/상태 |
|----|------|-----------|
| 1 | **A** docs + STAGE-WORKFLOW | `52f7298` (승격) |
| 2 | **M** Metal 3802 live | `52f7298` → `metal_3802.py` |
| 3 | **N** Non-Metal live | `52f7298` / `7360f93` → `non_metal*.py` |
| 4 | **F** PSP overlay | `52f7298` → `dmg_mount` / yellow_screen |

### 다음 INTEGRATE 큐 (명시 · 이 순서)

| 순 | 항목 | 비고 |
|----|------|------|
| 5 | **H** | IOSurface/CA extreme_open — N과 병행 허용 |
| 6 | **J** | 셰이더 AVX detect 큐 — 노란화면 ≠ Safari SIGILL |
| 7 | **D** | AGDC 검증 · `.stage-D` |
| 8 | **L5-R / B** | L5-R=루트볼륨 패치셋; B=`SL-BYTEPATCH-LUT`와 조율 |
| 9 | **I ↔ K** | I=`apply_live` ↔ K E2E extreme / `interpose_apply` |

영구 차단 문구 없음. `X86_EXTREME=1`이면 실험 허용 (기본 경로는 안전 `{}`).
```

### 3) 갭 표 A/M/N/F 상태 문자열

| 트랙 | 제안 상태 |
|------|-----------|
| **A** | connected · **INTEGRATE done** `52f7298` · 이후 `.stage-A`만 |
| **F** | landed · **INTEGRATE done** `52f7298` |
| **M** | landed · **INTEGRATE done** live |
| **N** | landed · **INTEGRATE done** live |

### 4) 변경 이력 행

```markdown
| 2026-09-04 | post-`52f7298`: A/M/N/F done · **다음 큐 H→J→D→L5-R/B→I↔K** (stage-A) |
```

---

## 트랙 A 메모

공유 Tracks/Research/STAGE-WORKFLOW 원본 터치 안 함.  
Research: `Tahoe-SkyLight-LUT-Research.md.stage-A`.  
STAGE-WORKFLOW touch-up: `STAGE-WORKFLOW.md.stage-A`.

---

## 변경 이력 (stage-A)

| 날짜 | 내용 |
|------|------|
| 2026-09-04 | post-INTEGRATE 큐 H→J→D→L5-R/B→I↔K |
