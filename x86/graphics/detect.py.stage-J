# Track J — proposed patch for x86/graphics/detect.py
# MC applies; Track J must not edit detect.py. Base: 52f7298.
# After payload.update(yellow) in serialize_graphics_detect_fields:

    from x86.graphics.shader_avx_gate import serialize_shader_avx_fields

    if "shader_avx" not in payload:
        payload.update(
            serialize_shader_avx_fields(
                cpu_has_avx1=report.has_avx1,
                cpu_has_avx2=report.has_avx2,
            )
        )
